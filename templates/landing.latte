{layout 'layouts/landing.latte'}

{block content}
<main class="landing">
    {* ─── 1. Hero ─── *}
    <section class="hero" id="hero">
        <div class="hero-grid">
            <div class="hero-content">
                <div class="hero-brand">
                    <span class="hero-brand-mark" aria-hidden="true">金</span>
                    <span class="hero-brand-text">Kintsugi</span>
                </div>
                <h1 class="hero-tagline">
                    Mend the wallet.<br>
                    <span class="accent">Keep the gold.</span>
                </h1>
                <p class="hero-sub">
                    {$leafDescription ?? 'Atomic, sweeper-proof wallet rescue using EIP-7702.'}
                    Open source. Runs locally. No fees, ever.
                </p>
                <div class="hero-cta-row">
                    <div class="install-box code-block-wrapper">
                        <span class="prompt">$</span>
                        <code id="install-cmd">npm install -g @kintsugi/cli</code>
                        <button type="button" class="copy-btn" aria-label="Copy install command" onclick="
                            navigator.clipboard.writeText('npm install -g @kintsugi/cli');
                            this.classList.add('copied');
                            setTimeout(() => this.classList.remove('copied'), 1600);
                        ">
                            <svg class="copy-icon" width="15" height="15" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path d="M8 2a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2V4a2 2 0 00-2-2H8zm-4 5a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2v-1H8a3 3 0 01-3-3V7H4z"/></svg>
                            <svg class="copy-check" width="15" height="15" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 10.5l3.5 3.5L16 5.5"/></svg>
                        </button>
                    </div>
                    <a href="{$leafBaseUrl}/getting-started/install/" class="btn-secondary">
                        Read the docs
                        <svg width="14" height="14" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"/></svg>
                    </a>
                </div>
            </div>
        </div>
    </section>

    {* ─── 2. The hostage problem ─── *}
    <section class="problem-section" id="problem">
        <div class="problem-grid">
            <div>
                <span class="section-eyebrow">The problem</span>
                <h2 class="section-title">Your keys are leaked. Your wallet is being watched.</h2>
                <p class="section-lede">
                    A sweeper bot now monitors the compromised address around the clock. The moment any ETH lands there to pay gas for a recovery transaction, the bot front-runs you and drains it in the same block. Tokens, NFTs, and ENS names sit visible but unreachable. Most rescues fail before they start.
                </p>
                <p class="problem-aside">
                    Kintsugi removes the gas dependency entirely. The victim wallet never holds ETH at any point. A separate rescuer wallet pays for and submits the recovery in one atomic Type-4 transaction, transferring every selected asset out before the sweeper sees an opening.
                </p>
            </div>
            <div>
                <figure class="attack-story" aria-label="The sweeper attack sequence">
                    <figcaption class="attack-story-eyebrow">The attack, in three beats</figcaption>
                    <ol class="attack-steps">
                        <li class="attack-step" data-step="1" style="--step-delay: 0s">
                            <span class="attack-step-index" aria-hidden="true">1</span>
                            <span class="attack-step-icon" aria-hidden="true">
                                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="6" width="18" height="13" rx="2"/><path d="M3 10h18"/><path d="M16 14.5h2"/><path d="M12 2v4"/><path d="M9 4l3 3 3-3"/></svg>
                            </span>
                            <div class="attack-step-body">
                                <p class="attack-step-title">You send ETH for gas</p>
                                <p class="attack-step-sub">0.01 ETH lands in the compromised wallet.</p>
                            </div>
                        </li>
                        <li class="attack-step is-attack" data-step="2" style="--step-delay: 2s">
                            <span class="attack-step-index" aria-hidden="true">2</span>
                            <span class="attack-step-icon" aria-hidden="true">
                                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="7" width="16" height="11" rx="3"/><path d="M9 12h.01"/><path d="M15 12h.01"/><path d="M12 3v4"/><path d="M8 18v2"/><path d="M16 18v2"/></svg>
                            </span>
                            <div class="attack-step-body">
                                <p class="attack-step-title">The sweeper bot grabs it instantly</p>
                                <p class="attack-step-sub">Front-runs in the same block. The 0.01 ETH is gone before you blink.</p>
                            </div>
                        </li>
                        <li class="attack-step is-attack" data-step="3" style="--step-delay: 4s">
                            <span class="attack-step-index" aria-hidden="true">3</span>
                            <span class="attack-step-icon" aria-hidden="true">
                                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M8 8l8 8"/><path d="M16 8l-8 8"/></svg>
                            </span>
                            <div class="attack-step-body">
                                <p class="attack-step-title">Your rescue transaction reverts</p>
                                <p class="attack-step-sub">Out of gas. The transfers never run.</p>
                            </div>
                        </li>
                    </ol>
                    <div class="attack-result">
                        <span class="attack-result-icon" aria-hidden="true">
                            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="11" width="16" height="10" rx="2"/><path d="M8 11V7a4 4 0 1 1 8 0v4"/><circle cx="12" cy="16" r="1.2" fill="currentColor"/></svg>
                        </span>
                        <div>
                            <p class="attack-result-title">Your assets remain trapped.</p>
                            <p class="attack-result-sub">Tokens, NFTs, and ENS names sit visible but unreachable. This is where Kintsugi steps in.</p>
                        </div>
                    </div>
                </figure>
            </div>
        </div>
    </section>

    {* ─── 2.5. Counter to the 7702-as-attack pattern ─── *}
    <section class="landing-section counter-section" id="counter">
        <span class="section-eyebrow">The attacker's other trick</span>
        <h2 class="section-title">When attackers use EIP-7702 against you, we use it back.</h2>
        <p class="section-lede">
            After a key compromise, attackers often install their own EIP-7702 delegation on your wallet, pointing it to an auto-drainer contract that triggers on any incoming funds, on any chain. Most "why does my wallet show delegation?" stories are actually this. Kintsugi's rescue uses the same primitive to overwrite their delegation, atomically, before our transaction does anything else.
        </p>
        <div class="counter-grid">
            <article class="counter-card counter-card--attack">
                <span class="counter-card-eyebrow">Their move</span>
                <h3 class="counter-card-title">Install an auto-drainer</h3>
                <ol class="counter-steps">
                    <li>
                        <span class="counter-step-num">1</span>
                        <span>Attacker has your private key.</span>
                    </li>
                    <li>
                        <span class="counter-step-num">2</span>
                        <span>Sign a 7702 authorization pointing your wallet at <code>AutoDrainer</code>.</span>
                    </li>
                    <li>
                        <span class="counter-step-num">3</span>
                        <span>Submit a Type-4 tx (attacker pays gas, no involvement from you).</span>
                    </li>
                </ol>
                <p class="counter-card-result">
                    Any incoming ETH is auto-stolen in the same block. Across every chain the bot watches.
                </p>
            </article>
            <article class="counter-card counter-card--defense">
                <span class="counter-card-eyebrow">Our move</span>
                <h3 class="counter-card-title">Overwrite with the Rescue contract</h3>
                <ol class="counter-steps">
                    <li>
                        <span class="counter-step-num">1</span>
                        <span>Sign a new 7702 authorization at the <em>current</em> nonce, pointing to <code>Rescue</code>.</span>
                    </li>
                    <li>
                        <span class="counter-step-num">2</span>
                        <span>One Type-4 tx: our authorization processes first, overwriting theirs.</span>
                    </li>
                    <li>
                        <span class="counter-step-num">3</span>
                        <span>Then the same tx calls <code>Rescue.executeBatch</code> on your wallet.</span>
                    </li>
                </ol>
                <p class="counter-card-result">
                    Their drainer never executes. Assets transferred atomically to your safe wallet.
                </p>
            </article>
        </div>
        <p class="counter-footnote">
            Newer EIP-7702 authorizations at the current nonce always win. That's the spec, not a workaround. Pair with <code>--private-mempool</code> when paranoia is warranted so the attacker can't even see our tx coming.
        </p>
    </section>

    {* ─── 3. What sweepers ignore (dark) ─── *}
    <section class="section-dark section-dark--a" id="coverage">
        <div class="landing-section">
            <span class="section-eyebrow">The recovery gap</span>
            <h2 class="section-title">They take what's liquid. They leave what's yours.</h2>
            <p class="section-lede">
                Drainer bots optimize for fast liquidation. They sweep ETH and the most liquid ERC-20s, ignore everything that takes effort to fence, and move on. The leftovers are often the most personally valuable holdings, but a working sweeper still blocks any normal recovery attempt.
            </p>
            <div class="cards-row">
                <div class="feature-card">
                    <div class="feature-card-icon" aria-hidden="true">字</div>
                    <h3>ENS names</h3>
                    <p>
                        Your dot-eth identity, the one you've used everywhere for years, is rarely auto-drained. It's also typically the hardest to move once a sweeper is watching. Kintsugi handles unwrapped 2LDs, wrapped names, and subdomains in a single batch.
                    </p>
                </div>
                <div class="feature-card">
                    <div class="feature-card-icon" aria-hidden="true">画</div>
                    <h3>Illiquid NFTs</h3>
                    <p>
                        Long-tail collections, art, in-game items, memberships. Bots usually skip ERC-721 and ERC-1155 because the resale path is slow. They stay on the compromised wallet, visible to everyone, recoverable by no one.
                    </p>
                </div>
                <div class="feature-card">
                    <div class="feature-card-icon" aria-hidden="true">小</div>
                    <h3>Low-cap tokens</h3>
                    <p>
                        Reward tokens, governance positions, vested allocations, anything without a liquid pool. Drainers ignore them, but you still cannot reach them through a wallet the sweeper is gating.
                    </p>
                </div>
            </div>
        </div>
    </section>

    {* ─── 4. How Kintsugi works (dark, slightly different shade) ─── *}
    <section class="section-dark section-dark--b" id="how">
        <div class="landing-section">
            <span class="section-eyebrow">How it works</span>
            <h2 class="section-title">One transaction. No gas from you. Nothing left behind.</h2>
            <p class="section-lede">
                EIP-7702 lets a victim wallet temporarily delegate execution to a battle-tested rescue contract. Combined with EIP-712 batch authorization, every selected asset moves to your safe wallet inside one atomic Type-4 transaction, paid for by a separate rescuer key.
            </p>
            <figure class="rescue-story" aria-label="The rescue sequence">
                <figcaption class="rescue-story-eyebrow">The rescue, in three beats</figcaption>
                <ol class="rescue-steps">
                    <li class="rescue-step" data-step="1" style="--step-delay: 0s">
                        <span class="rescue-step-index" aria-hidden="true">1</span>
                        <span class="rescue-step-icon" aria-hidden="true">
                            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M14.5 3.5l6 6L9 21H3v-6z"/><path d="M13 5l6 6"/><path d="M3 21h18"/></svg>
                        </span>
                        <div class="rescue-step-body">
                            <p class="rescue-step-title">You sign, locally.</p>
                            <p class="rescue-step-sub">An EIP-7702 authorization plus an EIP-712 batch of transfers (ERC-20, NFTs, ENS). Keys live in your local Node process only and are never written to disk.</p>
                        </div>
                    </li>
                    <li class="rescue-step" data-step="2" style="--step-delay: 2s">
                        <span class="rescue-step-index" aria-hidden="true">2</span>
                        <span class="rescue-step-icon" aria-hidden="true">
                            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M21 3L11 13"/><path d="M21 3L14.5 21l-3.5-8L3 9.5z"/></svg>
                        </span>
                        <div class="rescue-step-body">
                            <p class="rescue-step-title">Your rescuer submits one Type-4 transaction.</p>
                            <p class="rescue-step-sub">A separate wallet you control pays the gas. Your authorization processes first inside the tx, then it calls <code>Rescue.executeBatch</code> on your wallet.</p>
                        </div>
                    </li>
                    <li class="rescue-step" data-step="3" style="--step-delay: 4s">
                        <span class="rescue-step-index" aria-hidden="true">3</span>
                        <span class="rescue-step-icon" aria-hidden="true">
                            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 10h18"/><circle cx="12" cy="14.5" r="2"/><path d="M12 14.5v2"/></svg>
                        </span>
                        <div class="rescue-step-body">
                            <p class="rescue-step-title">Every asset lands at your safe wallet.</p>
                            <p class="rescue-step-sub">Same block, atomic. The sweeper sees the tx land, finds the wallet empty, and moves on.</p>
                        </div>
                    </li>
                </ol>
                <div class="rescue-result">
                    <span class="rescue-result-icon" aria-hidden="true">
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2l8 4v6c0 5-3.5 9-8 10-4.5-1-8-5-8-10V6z"/><path d="M9 12l2.2 2.2L15 10"/></svg>
                    </span>
                    <div>
                        <p class="rescue-result-title">Your balance never rose above zero.</p>
                        <p class="rescue-result-sub">There was no opening for a sweeper to act on, because there was nothing to take. After the rescue, optionally clear the delegation with <code>kintsugi revoke</code> to leave a pure EOA behind.</p>
                    </div>
                </div>
            </figure>
        </div>
    </section>

    {* ─── 5. UI / CLI showcase as real-feeling app windows ─── *}
    <section class="landing-section" id="try">
        <span class="section-eyebrow">Two ways in</span>
        <h2 class="section-title">A guided UI for panic. A pure CLI for power.</h2>
        <p class="section-lede">
            Same atomic flow under the hood, two surfaces to drive it. The web UI is launched from the CLI as a localhost server. The browser only sees session-bound addresses and signatures. Private keys never leave the local Node process.
        </p>
        <div class="usage-grid">
            <article class="usage-card">
                <div class="window">
                    <div class="window-chrome">
                        <span class="window-dots" aria-hidden="true">
                            <span class="window-dot window-dot--red"></span>
                            <span class="window-dot window-dot--yellow"></span>
                            <span class="window-dot window-dot--green"></span>
                        </span>
                        <span class="window-title">127.0.0.1:38080</span>
                        <span class="window-spacer"></span>
                    </div>
                    <div class="window-body window-body--ui">
                        <img src="{$leafBaseUrl}/assets/images/ui/welcome.png" alt="Kintsugi web UI welcome screen with the Kintsugi wordmark and the tagline Mend the wallet, Keep the gold." loading="lazy" width="1440" height="900">
                    </div>
                </div>
                <div class="usage-card-meta">
                    <span class="usage-card-eyebrow">The web UI</span>
                    <h3 class="usage-card-title">Guided. Step by step.</h3>
                    <ul class="usage-card-bullets">
                        <li>Pick the network and your RPC.</li>
                        <li>Paste victim, safe, and rescuer wallets.</li>
                        <li>Pick what to rescue, sign, submit.</li>
                    </ul>
                    <a href="{$leafBaseUrl}/ui-walkthrough/network/" class="usage-card-cta">Walk through the UI →</a>
                </div>
            </article>

            <article class="usage-card">
                <div class="window window--terminal">
                    <div class="window-chrome window-chrome--terminal">
                        <span class="window-dots" aria-hidden="true">
                            <span class="window-dot window-dot--red"></span>
                            <span class="window-dot window-dot--yellow"></span>
                            <span class="window-dot window-dot--green"></span>
                        </span>
                        <span class="window-title window-title--terminal">~  kintsugi rescue</span>
                        <span class="window-spacer"></span>
                    </div>
                    <div class="window-body window-body--terminal">
                        <pre class="window-terminal-pre"><span class="comment"># launch the guided localhost UI</span>
<span class="prompt">$</span> kintsugi ui

<span class="comment"># ...or do everything in the terminal</span>
<span class="prompt">$</span> kintsugi rescue

<span class="comment"># inspect a wallet read-only first</span>
<span class="prompt">$</span> kintsugi status 0xVictim...

<span class="comment"># after rescue, optionally clear delegation</span>
<span class="prompt">$</span> kintsugi revoke
<span class="cursor" aria-hidden="true">▍</span></pre>
                    </div>
                </div>
                <div class="usage-card-meta">
                    <span class="usage-card-eyebrow">The CLI</span>
                    <h3 class="usage-card-title">Headless. Scriptable.</h3>
                    <ul class="usage-card-bullets">
                        <li>One command (<code>kintsugi rescue</code>) walks you through.</li>
                        <li>Read-only inventory with <code>kintsugi status</code>.</li>
                        <li>Pipes well. No browser required.</li>
                    </ul>
                    <a href="{$leafBaseUrl}/cli-reference/rescue/" class="usage-card-cta">Read the CLI reference →</a>
                </div>
            </article>
        </div>
    </section>

    {* ─── 5.5 The third way in: ask your agent ─── *}
    <section class="landing-section agent-section" id="agent">
        <span class="section-eyebrow">The third way in</span>
        <h2 class="section-title">In panic? Don't read. <span class="accent">Ask your agent.</span></h2>
        <p class="section-lede">
            Kintsugi ships a free agent skill following the <a href="https://agentskills.io" target="_blank" rel="noopener">agentskills.io</a> spec. Install it once and your favourite AI coding agent learns the three-wallet pattern, the ordering rules, and every gotcha. Then it walks you through a rescue in plain English.
        </p>

        <div class="agent-conversation" aria-label="Example conversation with an agent">
            <div class="chat-message chat-message--user">
                <div class="chat-bubble">
                    <p class="chat-bubble-text">My wallet got drained but I have hostage assets there. Help me rescue them with Kintsugi.</p>
                    <button type="button" class="chat-bubble-copy" aria-label="Copy this prompt" onclick="
                        navigator.clipboard.writeText('My wallet got drained but I have hostage assets there. Help me rescue them with Kintsugi.');
                        this.classList.add('copied');
                        setTimeout(() => this.classList.remove('copied'), 1600);
                    ">
                        <svg class="copy-icon" width="14" height="14" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path d="M8 2a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2V4a2 2 0 00-2-2H8zm-4 5a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2v-1H8a3 3 0 01-3-3V7H4z"/></svg>
                        <svg class="copy-check" width="14" height="14" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 10.5l3.5 3.5L16 5.5"/></svg>
                    </button>
                </div>
                <div class="chat-meta">
                    <span class="chat-avatar chat-avatar--user" aria-hidden="true">You</span>
                </div>
            </div>

            <div class="chat-message chat-message--agent">
                <div class="chat-meta">
                    <span class="chat-avatar chat-avatar--agent" aria-hidden="true">金</span>
                </div>
                <div class="chat-bubble chat-bubble--agent">
                    <p class="chat-bubble-text">Got it. I'll walk you through this end to end. First, what chain is the compromised wallet on, and do you have a fresh wallet ready to receive the assets? If not, I can generate one for you in a moment.</p>
                    <p class="chat-bubble-meta">Powered by the Kintsugi agent skill</p>
                </div>
            </div>
        </div>

        <div class="skill-sidecar">
            <div class="skill-sidecar-row">
                <div class="skill-sidecar-block">
                    <p class="skill-sidecar-label">Install the skill once:</p>
                    <div class="install-box code-block-wrapper">
                        <span class="prompt">$</span>
                        <code id="install-skill-cmd">npx skills add ophelios-studio/skills --skill kintsugi</code>
                        <button type="button" class="copy-btn" aria-label="Copy install command" onclick="
                            navigator.clipboard.writeText('npx skills add ophelios-studio/skills --skill kintsugi');
                            this.classList.add('copied');
                            setTimeout(() => this.classList.remove('copied'), 1600);
                        ">
                            <svg class="copy-icon" width="15" height="15" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path d="M8 2a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2V4a2 2 0 00-2-2H8zm-4 5a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2v-1H8a3 3 0 01-3-3V7H4z"/></svg>
                            <svg class="copy-check" width="15" height="15" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 10.5l3.5 3.5L16 5.5"/></svg>
                        </button>
                    </div>
                </div>
                <div class="skill-sidecar-block">
                    <p class="skill-sidecar-label">Works with:</p>
                    <ul class="agent-clients">
                        <li class="agent-client-chip">Claude Code</li>
                        <li class="agent-client-chip">Cursor</li>
                        <li class="agent-client-chip">Codex</li>
                        <li class="agent-client-chip">Cline</li>
                        <li class="agent-client-chip">Gemini CLI</li>
                    </ul>
                </div>
            </div>
            <div class="skill-sidecar-cta">
                <a href="{$leafBaseUrl}/getting-started/with-an-agent/" class="btn-secondary">
                    See what else to ask
                    <svg width="14" height="14" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"/></svg>
                </a>
            </div>
        </div>
    </section>

    {* ─── 7. Get-started lanes (last section in main) ─── *}
    <section class="section-light" id="start-wrap">
        <div class="landing-section" id="start">
            <span class="section-eyebrow">Three ways forward</span>
            <h2 class="section-title">Where do you want to start?</h2>
            <div class="lanes-row">
                <a href="{$leafBaseUrl}/getting-started/quickstart-cli/" class="lane-card lane-card--panic">
                    <span class="lane-icon" aria-hidden="true">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 7v6"/><circle cx="12" cy="16.5" r="0.6" fill="currentColor"/></svg>
                    </span>
                    <span class="lane-eyebrow">Right now</span>
                    <h3 class="lane-title">I'm in trouble. Walk me through it.</h3>
                    <p class="lane-body">Five minutes from install to assets safely in a fresh wallet. Plain language, no Solidity required.</p>
                    <span class="lane-cta">Five-minute rescue →</span>
                </a>
                <a href="{$leafBaseUrl}/concepts/the-sweeper-problem/" class="lane-card lane-card--learn">
                    <span class="lane-icon" aria-hidden="true">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 5a2 2 0 0 1 2-2h6v18H5a2 2 0 0 1-2-2z"/><path d="M21 5a2 2 0 0 0-2-2h-6v18h6a2 2 0 0 0 2-2z"/><path d="M11 7H6"/><path d="M11 11H6"/><path d="M18 7h-5"/><path d="M18 11h-5"/></svg>
                    </span>
                    <span class="lane-eyebrow">First</span>
                    <h3 class="lane-title">I want to understand it first.</h3>
                    <p class="lane-body">The sweeper problem, EIP-7702 in plain English, the rescuer wallet pattern, why the rescue is atomic.</p>
                    <span class="lane-cta">Read the concepts →</span>
                </a>
                <a href="{$leafBaseUrl}/security/threat-model/" class="lane-card lane-card--audit">
                    <span class="lane-icon" aria-hidden="true">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2l8 4v6c0 5-3.5 9-8 10-4.5-1-8-5-8-10V6z"/><path d="M9 12l2.2 2.2L15 10"/></svg>
                    </span>
                    <span class="lane-eyebrow">Auditor mode</span>
                    <h3 class="lane-title">Show me the trust assumptions.</h3>
                    <p class="lane-body">Threat model, what the rescuer can and cannot do, known limits, how to report a vulnerability.</p>
                    <span class="lane-cta">Read the security docs →</span>
                </a>
            </div>
        </div>
    </section>
</main>

{* ─── Outro reveal: Our Stance + footer.
   These are OUTSIDE <main> so they can sit fixed at the bottom of the viewport,
   revealed as the user scrolls past the main content. Both share a white surface
   so they merge seamlessly into one closing block. *}
{block footer}
<div class="outro-reveal" id="outro-reveal">
    <section class="landing-section outro-section" id="open">
            <div class="manifesto">
                <span class="section-eyebrow">Our stance</span>
                <h2 class="manifesto-title">
                    Open source. No fees. <span class="accent">No upsell.</span>
                </h2>
                <p class="manifesto-lede">
                    A wallet rescue tool that charges money in the worst moment of someone's crypto life is a wallet rescue tool taking advantage of panic. We will not do that.
                </p>
            </div>

            <div class="principles-row">
                <article class="principle-card">
                    <span class="principle-card-icon" aria-hidden="true">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="11" width="16" height="10" rx="2"/><path d="M8 11V7a4 4 0 0 1 8 0"/></svg>
                    </span>
                    <h3 class="principle-card-title">Open source.</h3>
                    <p class="principle-card-body">MIT-licensed. Public repository. Every line auditable, every contract verified.</p>
                </article>
                <article class="principle-card">
                    <span class="principle-card-icon" aria-hidden="true">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M5 5l14 14"/><path d="M9 12h6"/></svg>
                    </span>
                    <h3 class="principle-card-title">No fees.</h3>
                    <p class="principle-card-body">No SaaS tier. No signup. No telemetry. Pay only your own gas.</p>
                </article>
                <article class="principle-card">
                    <span class="principle-card-icon" aria-hidden="true">
                        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 10l9-6 9 6v10a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><path d="M3 10h18"/><path d="M9 22V14h6v8"/></svg>
                    </span>
                    <h3 class="principle-card-title">No upsell.</h3>
                    <p class="principle-card-body">No premium features behind a paywall. The whole tool is the whole tool.</p>
                </article>
            </div>

            <div class="deployments-panel">
                <header class="deployments-panel-head">
                    <span class="deployments-panel-eyebrow">Deployments</span>
                    <a n:if="($leafGithubUrl ?? '') !== ''" href="{$leafGithubUrl}" target="_blank" rel="noopener" class="deployments-panel-source">
                        View source on GitHub
                        <svg width="12" height="12" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"/></svg>
                    </a>
                </header>
                <div class="deployments-rows">
                    <div class="deployment-network">
                        <span class="deployment-network-name">Sepolia</span>
                        <span class="deployment-network-status deployment-network-status--ok">Verified</span>
                    </div>
                    <div class="deployment-row">
                        <span class="deployment-row-label">Rescue contract</span>
                        <code class="deployment-address" id="addr-sepolia-rescue">0x53c1f40ca0a58942f9eb89d7fd445457a8521fd5</code>
                        <button type="button" class="deployment-copy" aria-label="Copy Sepolia Rescue address" onclick="
                            navigator.clipboard.writeText('0x53c1f40ca0a58942f9eb89d7fd445457a8521fd5');
                            this.classList.add('copied');
                            setTimeout(() => this.classList.remove('copied'), 1500);
                        ">
                            <svg class="copy-icon" width="13" height="13" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path d="M8 2a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2V4a2 2 0 00-2-2H8zm-4 5a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2v-1H8a3 3 0 01-3-3V7H4z"/></svg>
                            <svg class="copy-check" width="13" height="13" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 10.5l3.5 3.5L16 5.5"/></svg>
                        </button>
                    </div>
                    <div class="deployment-row">
                        <span class="deployment-row-label">NonceTracker</span>
                        <code class="deployment-address" id="addr-sepolia-tracker">0x717883abfa58fa2bf0f9c2d5a132227253c47963</code>
                        <button type="button" class="deployment-copy" aria-label="Copy Sepolia NonceTracker address" onclick="
                            navigator.clipboard.writeText('0x717883abfa58fa2bf0f9c2d5a132227253c47963');
                            this.classList.add('copied');
                            setTimeout(() => this.classList.remove('copied'), 1500);
                        ">
                            <svg class="copy-icon" width="13" height="13" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path d="M8 2a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2V4a2 2 0 00-2-2H8zm-4 5a2 2 0 00-2 2v9a2 2 0 002 2h7a2 2 0 002-2v-1H8a3 3 0 01-3-3V7H4z"/></svg>
                            <svg class="copy-check" width="13" height="13" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 10.5l3.5 3.5L16 5.5"/></svg>
                        </button>
                    </div>
                    <div class="deployment-network deployment-network--pending">
                        <span class="deployment-network-name">Mainnet</span>
                        <span class="deployment-network-status deployment-network-status--pending">Pending audit</span>
                    </div>
                </div>
            </div>
        </section>

        {* The white footer that merges seamlessly with the open section above *}
        <footer class="site-footer site-footer--reveal">
            <div class="landing-section">
                <div class="reveal-footer-grid">
                    <div class="reveal-footer-brand">
                        <a href="{$leafBaseUrl}/" class="reveal-footer-logo" aria-label="Kintsugi home">
                            <span class="reveal-footer-mark" aria-hidden="true">金</span>
                            <span class="reveal-footer-name">Kintsugi</span>
                        </a>
                        <p class="reveal-footer-tag">Mend the wallet. Keep the gold.</p>
                        <p class="reveal-footer-by">
                            Built by
                            <a n:if="($leafAuthorUrl ?? '') !== ''" href="{$leafAuthorUrl}" target="_blank" rel="noopener">{$leafAuthor ?? 'Ophelios Studio'}</a>
                            <span n:if="($leafAuthorUrl ?? '') === ''">{$leafAuthor ?? 'Ophelios Studio'}</span>
                        </p>
                    </div>
                    <nav class="reveal-footer-cols" aria-label="Footer">
                        <div class="reveal-footer-col">
                            <h4>Product</h4>
                            <ul>
                                <li><a href="{$leafBaseUrl}/getting-started/install/">Install</a></li>
                                <li><a href="{$leafBaseUrl}/cli-reference/rescue/">CLI</a></li>
                                <li><a href="{$leafBaseUrl}/ui-walkthrough/network/">UI walkthrough</a></li>
                                <li><a href="{$leafBaseUrl}/getting-started/with-an-agent/">Agent skill</a></li>
                            </ul>
                        </div>
                        <div class="reveal-footer-col">
                            <h4>Concepts</h4>
                            <ul>
                                <li><a href="{$leafBaseUrl}/concepts/the-sweeper-problem/">The sweeper problem</a></li>
                                <li><a href="{$leafBaseUrl}/concepts/eip-7702-explained/">EIP-7702 explained</a></li>
                                <li><a href="{$leafBaseUrl}/concepts/rescuer-pattern/">Rescuer pattern</a></li>
                                <li><a href="{$leafBaseUrl}/concepts/atomic-batches/">Atomic batches</a></li>
                            </ul>
                        </div>
                        <div class="reveal-footer-col">
                            <h4>Security</h4>
                            <ul>
                                <li><a href="{$leafBaseUrl}/security/threat-model/">Threat model</a></li>
                                <li><a href="{$leafBaseUrl}/security/trust-assumptions/">Trust assumptions</a></li>
                                <li><a href="{$leafBaseUrl}/security/known-limits/">Known limits</a></li>
                                <li><a href="{$leafBaseUrl}/security/reporting/">Report a vulnerability</a></li>
                            </ul>
                        </div>
                        <div class="reveal-footer-col">
                            <h4>Project</h4>
                            <ul>
                                <li><a n:if="($leafGithubUrl ?? '') !== ''" href="{$leafGithubUrl}" target="_blank" rel="noopener">GitHub</a></li>
                                <li><a href="https://github.com/ophelios-studio/skills" target="_blank" rel="noopener">Agent skills repo</a></li>
                                <li><a n:if="($leafAuthorUrl ?? '') !== ''" href="{$leafAuthorUrl}" target="_blank" rel="noopener">Ophelios Studio</a></li>
                                <li><a href="{$leafBaseUrl}/faq/index/">FAQ</a></li>
                            </ul>
                        </div>
                    </nav>
                </div>
                <div class="reveal-footer-bottom">
                    <span>v{$leafVersion ?? '0.1.0'} pre-alpha</span>
                    <span class="reveal-footer-bottom-spacer" aria-hidden="true"></span>
                    <span>{$leafLicense ?? 'MIT'} License</span>
                    <span>&copy; 2026 Ophelios Studio</span>
                </div>
                <div class="reveal-footer-mark-bg" aria-hidden="true">金</div>
            </div>
        </footer>
</div>

{* Size main's bottom margin to match the reveal height so the page can scroll
   far enough to fully uncover the reveal. Re-runs on resize. *}
<script>
(function(){
  function fit(){
    var main = document.querySelector('main.landing');
    var reveal = document.getElementById('outro-reveal');
    if (!main || !reveal) return;
    main.style.marginBottom = reveal.offsetHeight + 'px';
  }
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', fit);
  } else {
    fit();
  }
  window.addEventListener('load', fit);
  window.addEventListener('resize', fit);
})();
</script>
{/block}
